PyYAML>=6.0
pypandoc-binary>=1.15

# 注意：requirements.txt 只能安装 Python 包，不能安装 apt/npm 系统依赖。
# Ubuntu/WSL 用户请运行：
#   bash scripts/install_system_deps_ubuntu.sh
# 前端依赖请在 workflows/webui/frontend 中运行 npm install。
