# Thesis Source

This directory is user/project-specific and is ignored by git except for this README and `.gitkeep`.

Expected local contents:

```text
thesis/
  outline.md
  style.md
  state.json
  sections/
  logs/
```

For a new thesis project, create or copy these files locally, then update `configs/default.yaml` if your section filenames or output paths differ.
